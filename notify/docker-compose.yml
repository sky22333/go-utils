services:
  notify:
    image: ghcr.io/sky22333/go-utils:notify-latest
    container_name: notify
    restart: always
    ports:
      - "7000:7000"
    volumes:
      - ./config.yaml:/app/config.yaml
